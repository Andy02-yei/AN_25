---
title: "Tarea 5. Diferenciación e integración numérica."
author: "Zamora Pérez Andrea Anaeli"
format: 
  html:
    grid: 
      body-width: 1000px
editor: visual
jupyter: python3
---

Importamos packages y funciones necesarias:

```{python}
#| code-fold: true

import matplotlib.pyplot as plt
import numpy as np
import math
from scipy.interpolate import lagrange
from numpy.polynomial.polynomial import Polynomial
from scipy.interpolate import CubicSpline

import plotly.graph_objects as go
from scipy.differentiate import derivative
import numdifftools as nd
from scipy.stats import norm
from scipy import integrate

```

# Ejercicio 1.

Para cada una de las siguientes funciones:

-   Realiza la respectiva gráfica en el intervalo dado.

-   Compara las gráficas de las derivadas aproximadas de la función `derivative` de `Scipy`, con dos tamaños de paso utilizando la función `nd.Derivative` y la derivada *exacta* en tal intervalo.

-   Compara las gráficas de las segundas derivadas aproximadas con dos tamaños de paso utilizando la función `nd.Derivative` y la segunda derivada *exacta* en tal intervalo.

-   Realiza las gráficas de los errores absolutos en cada caso.

a)  $f(x)=e^{2x}-cos 2x$, $x\in [0,2]$

    ```{python}
#| code-fold: true
#| fig-align: 'center'

f= lambda x: np.exp(2*x)-np.cos(2*x)
    
x_val = np.linspace(0, 2, 100)

plt.figure(figsize=(8,6))
plt.plot(x_val,  f(x_val), color="thistle", linewidth=2)
plt.grid()
plt.show()
    ```

Derivada : $f'(x)= 2e^{2x} + 2sin(2x)$. Aproximaciones con dos tamaños de paso $h=0.02$ y $h=0.15$

```{python}
#| code-fold: true
#| warning: false
#| message: false

derf = lambda x: 2*np.exp(2*x) + 2*np.sin(2*x)

df_02 = nd.Derivative(f, step=0.02, method='central', order=2)
df_15 = nd.Derivative(f, step=0.15, method='central', order=2)

fig = go.Figure()
fig.add_trace(go.Scatter(x= x_val, y= derf(x_val), mode='lines', name='Derivada', line=dict(color='goldenrod', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= df_02(x_val), mode='lines', name='h=0.02', line=dict(color='teal', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= df_15(x_val), mode='lines', name='h=0.15', line=dict(color='royalblue', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= derivative(f, x_val).df, mode='lines', name='SciPy', line=dict(color='aqua', width=2)))

fig.update_layout(
    title="Gráfica de aproximación de las derivadas",
    xaxis_title="x",
    yaxis_title="y",
    template="plotly_white",
    width = 768,
    height = 576
)

fig.show()
```

Gráfica del valor absoluto de los errores para las aproximaciones de la primera derivada.

```{python}
#| code-fold: true
#| warning: false

fig = go.Figure()
fig.add_trace(go.Scatter(x= x_val, y= abs(derf(x_val)-df_02(x_val)), mode='lines', name='h=0.02', line=dict(color='royalblue', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= abs(derf(x_val)-df_15(x_val)), mode='lines', name='h=0.15', line=dict(color='teal', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= abs(derf(x_val)-derivative(f, x_val).df), mode='lines', name='SciPy', line=dict(color='aqua', width=2)))

fig.update_layout(
    title="Gráfica de errores",
    xaxis_title="x",
    yaxis_title="y",
    template="plotly_white",
    width = 768,
    height = 576
)

fig.show()
```

Segunda derivada: $f''(x)= 4e^{2x} + 4cos(2x)$. Aproximaciones con $h=0.02$ y $h=0.15$

```{python}
#| code-fold: true
#| warning: false

dderf = lambda x: 4*np.exp(2*x) + 4*np.cos(2*x)
ddf_02 = nd.Derivative(f, step=0.02, method='central', order=2, n = 2)
ddf_15 = nd.Derivative(f, step=0.15, method='central', order=2, n = 2)

fig = go.Figure()
fig.add_trace(go.Scatter(x= x_val, y= ddf_02(x_val), mode='lines', name='h=0.02', line=dict(color='royalblue', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= ddf_15(x_val), mode='lines', name='h=0.15', line=dict(color='teal', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= dderf(x_val), mode='lines', name='segunda derivada', line=dict(color='goldenrod', width=1)))

fig.update_layout(
    title="Gráfica de aproximación de la segunda derivada",
    xaxis_title="x",
    yaxis_title="y",
    template="plotly_white",
    width = 768,
    height = 576
)

fig.show()
```

Gráfica del valor absoluto de los errores para las aproximaciones de la segunda derivada.

```{python}
#| code-fold: true
#| warning: false

fig = go.Figure()
fig.add_trace(go.Scatter(x= x_val, y= abs(dderf(x_val)-ddf_02(x_val)), mode='lines', name='h=0.02', line=dict(color='royalblue', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= abs(dderf(x_val)-ddf_15(x_val)), mode='lines', name='h=0.15', line=dict(color='teal', width=1)))

fig.update_layout(
    title="Gráfica de errores segunda derivada",
    xaxis_title="x",
    yaxis_title="y",
    template="plotly_white",
    width = 768,
    height = 576
)

fig.show()
```

b)  $f(x)=log(x+2)-(x+1)^2$, $x\in [0,5]$

    ```{python}
#| code-fold: true
#| fig-align: 'center'

f= lambda x: np.log(x+2) - (x+1)**2
    
x_val = np.linspace(0, 5, 400)

plt.figure(figsize=(8,6))
plt.plot(x_val,  f(x_val), color="firebrick", linewidth=2)
plt.grid()
plt.show()
    ```

Derivada : $f'(x)= -2x+\frac{1}{x+2}-2$. Aproximaciones con dos tamaños de paso $h=0.12$ y $h=0.25$

```{python}
#| code-fold: true
#| warning: false
#| message: false

derf = lambda x: -2*x + ((1)/(x+2)) -2

df_12 = nd.Derivative(f, step=0.12, method='central', order=2)
df_25 = nd.Derivative(f, step=0.25, method='central', order=2)

fig = go.Figure()
fig.add_trace(go.Scatter(x= x_val, y= derf(x_val), mode='lines', name='Derivada', line=dict(color='goldenrod', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= df_12(x_val), mode='lines', name='h=0.12', line=dict(color='teal', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= df_25(x_val), mode='lines', name='h=0.25', line=dict(color='royalblue', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= derivative(f, x_val).df, mode='lines', name='SciPy', line=dict(color='aqua', width=2)))

fig.update_layout(
    title="Gráfica de aproximación de las derivadas",
    xaxis_title="x",
    yaxis_title="y",
    template="plotly_white",
    width = 768,
    height = 576
)

fig.show()
```

Gráfica del valor absoluto de los errores para las aproximaciones de la primera derivada.

```{python}
#| code-fold: true
#| warning: false

fig = go.Figure()
fig.add_trace(go.Scatter(x= x_val, y= abs(derf(x_val)-df_12(x_val)), mode='lines', name='h=0.12', line=dict(color='royalblue', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= abs(derf(x_val)-df_25(x_val)), mode='lines', name='h=0.25', line=dict(color='teal', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= abs(derf(x_val)-derivative(f, x_val).df), mode='lines', name='SciPy', line=dict(color='aqua', width=2)))

fig.update_layout(
    title="Gráfica de errores",
    xaxis_title="x",
    yaxis_title="y",
    template="plotly_white",
    width = 768,
    height = 576
)

fig.show()
```

Segunda derivada: $f''(x)= -\frac{1}{(x+2)^2}-2$. Aproximaciones con $h=0.12$ y $h=0.25$

```{python}
#| code-fold: true
#| warning: false

dderf = lambda x: -(1/(x+2)**2) -2
ddf_12 = nd.Derivative(f, step=0.12, method='central', order=2, n = 2)
ddf_25 = nd.Derivative(f, step=0.25, method='central', order=2, n = 2)

fig = go.Figure()
fig.add_trace(go.Scatter(x= x_val, y= ddf_12(x_val), mode='lines', name='h=0.12', line=dict(color='royalblue', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= ddf_25(x_val), mode='lines', name='h=0.25', line=dict(color='teal', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= dderf(x_val), mode='lines', name='segunda derivada', line=dict(color='goldenrod', width=1)))

fig.update_layout(
    title="Gráfica de aproximación de la segunda derivada",
    xaxis_title="x",
    yaxis_title="y",
    template="plotly_white",
    width = 768,
    height = 576
)

fig.show()
```

Gráfica del valor absoluto de los errores para las aproximaciones de la segunda derivada.

```{python}
#| code-fold: true
#| warning: false

fig = go.Figure()
fig.add_trace(go.Scatter(x= x_val, y= abs(dderf(x_val)-ddf_12(x_val)), mode='lines', name='h=0.12', line=dict(color='royalblue', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= abs(dderf(x_val)-ddf_25(x_val)), mode='lines', name='h=0.25', line=dict(color='teal', width=1)))

fig.update_layout(
    title="Gráfica de errores segunda derivada",
    xaxis_title="x",
    yaxis_title="y",
    template="plotly_white",
    width = 768,
    height = 576
)

fig.show()
```

c)  $f(x)=\sqrt{x} sen(x^2)$, $x\in [0,\pi]$

Gráfica de la función.

```{python}
#| code-fold: true
#| fig-align: 'center'


f = lambda x: np.sqrt(x)*np.sin(x**2)

x_values = np.linspace(0, np.pi, 200)

plt.figure(figsize=(8,6))
plt.plot(x_values,  f(x_values), color = "darkred", linewidth=1.5)
plt.grid()
plt.show()
```

Derivada : $f'(x)= 2x\sqrt{x}\,cos(x^2)+\frac{sen(x^2)}{2\sqrt{x}}$. Aproximaciones con dos tamaños de paso $h=0.05$ y $h=0.1$

```{python}
#| code-fold: true
#| warning: false
#| message: false

derf = lambda x: 2* x * np.sqrt(x) * np.cos(x**2)+ (np.sin(x**2)/2) * (1/np.sqrt(x))

# Función de numdifftools
df_01 = nd.Derivative(f, step=0.1, method='central', order=2)
df_005 = nd.Derivative(f, step=0.05, method='central', order=2)


fig = go.Figure()
# Aproximación de la derivada con los tamaños de paso 
fig.add_trace(go.Scatter(x= x_values, y= df_01(x_values), mode='lines', name='h=0.1', line=dict(color='teal', width=1)))
fig.add_trace(go.Scatter(x= x_values, y= df_005(x_values), mode='lines', name='h=0.05', line=dict(color='royalblue', width=1)))
# Aproximación de la derivada con derivative de Scipy
fig.add_trace(go.Scatter(x= x_values, y= derivative(f, x_values).df, mode='lines', name='SciPy', line=dict(color='aqua', width=2)))
# Derivada "exacta"
fig.add_trace(go.Scatter(x= x_values, y= derf(x_values), mode='lines', name='Derivada', line=dict(color='goldenrod', width=1)))

fig.update_layout(
    title="Gráfica de aproximación de las derivadas",
    xaxis_title="x",
    yaxis_title="y",
    template="plotly_white",
    width = 768,
    height = 576
)

fig.show()
```

Gráfica del valor absoluto de los errores para las aproximaciones de la primera derivada.

```{python}
#| code-fold: true
#| warning: false


fig = go.Figure()

fig.add_trace(go.Scatter(x= x_values, y= abs(derf(x_values)-df_01(x_values)), mode='lines', name='h=0.1', line=dict(color='royalblue', width=1)))
fig.add_trace(go.Scatter(x= x_values, y= abs(derf(x_values)-df_005(x_values)), mode='lines', name='h=0.05', line=dict(color='teal', width=1)))
fig.add_trace(go.Scatter(x= x_values, y= abs(derf(x_values)-derivative(f, x_values).df), mode='lines', name='SciPy', line=dict(color='aqua', width=2)))

fig.update_layout(
    title="Gráfica de errores",
    xaxis_title="x",
    yaxis_title="y",
    template="plotly_white",
    width = 768,
    height = 576
)

fig.show()
```

Segunda derivada: $f''(x)= 4 \sqrt{x}\, cos(x^2)-sen(x^2)\left(4 x^2 \sqrt{x}+\frac{1}{4x\sqrt{x}} \right)$. Aproximaciones con $h=0.05$ y $h=0.1$

```{python}
#| code-fold: true
#| warning: false

dderf = lambda x: 4* np.sqrt(x) * np.cos(x**2) -np.sin(x**2) *(4* x **2 * np.sqrt(x)+1/(4*x*np.sqrt(x)))

# Funciones de numdifftools para la segunda derivada
ddf_01 = nd.Derivative(f, step=0.1, method='central', order=2, n = 2)
ddf_005 = nd.Derivative(f, step=0.05, method='central', order=2, n = 2)

fig = go.Figure()
fig.add_trace(go.Scatter(x= x_values, y= ddf_01(x_values), mode='lines', name='h=0.1', line=dict(color='royalblue', width=1)))
fig.add_trace(go.Scatter(x= x_values, y= ddf_005(x_values), mode='lines', name='h=0.05', line=dict(color='teal', width=1)))
fig.add_trace(go.Scatter(x= x_values, y= dderf(x_values), mode='lines', name='2da. derivada', line=dict(color='goldenrod', width=1)))

# Configurar diseño de la gráfica
fig.update_layout(
    title="Gráfica de aproximación de la 2da derivada",
    xaxis_title="x",
    yaxis_title="y",
    template="plotly_white",
    width = 768,
    height = 576
)

fig.show()

```

Gráfica del valor absoluto de los errores para las aproximaciones de la segunda derivada.

```{python}
#| code-fold: true
#| warning: false


fig = go.Figure()

fig.add_trace(go.Scatter(x= x_values, y= abs(dderf(x_values)-ddf_01(x_values)), mode='lines', name='h=0.1', line=dict(color='royalblue', width=1)))
fig.add_trace(go.Scatter(x= x_values, y= abs(dderf(x_values)-ddf_005(x_values)), mode='lines', name='h=0.05', line=dict(color='teal', width=1)))

fig.update_layout(
    title="Gráfica de errores segunda derivada",
    xaxis_title="x",
    yaxis_title="y",
    template="plotly_white",
    width = 768,
    height = 576
)

fig.show()
```

d)  $f(x)=(cos\,3x)^2-e^{2x}$, $x\in [0,\pi/2]$

    ```{python}
#| code-fold: true
#| fig-align: 'center'

f= lambda x: (np.cos(3*x))**2 - np.exp(2*x)
    
x_val = np.linspace(0, np.pi/2, 400)

plt.figure(figsize=(8,6))
plt.plot(x_val,  f(x_val), color="powderblue", linewidth=2)
plt.grid()
plt.show()
    ```

Derivada : $f'(x)= -3sin(6x)-e^{2x}2$. Aproximaciones con dos tamaños de paso $h=0.13$ y $h=0.22$

```{python}
#| code-fold: true
#| warning: false
#| message: false

derf = lambda x: -3*np.sin(6*x) - np.exp(2*x)*2

df_13 = nd.Derivative(f, step=0.13, method='central', order=2)
df_22 = nd.Derivative(f, step=0.22, method='central', order=2)

fig = go.Figure()
fig.add_trace(go.Scatter(x= x_val, y= derf(x_val), mode='lines', name='Derivada', line=dict(color='goldenrod', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= df_13(x_val), mode='lines', name='h=0.13', line=dict(color='teal', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= df_22(x_val), mode='lines', name='h=0.22', line=dict(color='royalblue', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= derivative(f, x_val).df, mode='lines', name='SciPy', line=dict(color='aqua', width=2)))

fig.update_layout(
    title="Gráfica de aproximación de las derivadas",
    xaxis_title="x",
    yaxis_title="y",
    template="plotly_white",
    width = 768,
    height = 576
)

fig.show()
```

Gráfica del valor absoluto de los errores para las aproximaciones de la primera derivada.

```{python}
#| code-fold: true
#| warning: false

fig = go.Figure()
fig.add_trace(go.Scatter(x= x_val, y= abs(derf(x_val)-df_13(x_val)), mode='lines', name='h=0.13', line=dict(color='royalblue', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= abs(derf(x_val)-df_22(x_val)), mode='lines', name='h=0.22', line=dict(color='teal', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= abs(derf(x_val)-derivative(f, x_val).df), mode='lines', name='SciPy', line=dict(color='aqua', width=2)))

fig.update_layout(
    title="Gráfica de errores",
    xaxis_title="x",
    yaxis_title="y",
    template="plotly_white",
    width = 768,
    height = 576
)

fig.show()
```

Segunda derivada: $f''(x)= -18cos(6x)-4e^{2x}$. Aproximaciones con $h=0.13$ y $h=0.22$

```{python}
#| code-fold: true
#| warning: false

dderf = lambda x: -18*np.cos(6*x)-4*np.exp(2*x)
ddf_13 = nd.Derivative(f, step=0.13, method='central', order=2, n = 2)
ddf_22 = nd.Derivative(f, step=0.22, method='central', order=2, n = 2)

fig = go.Figure()
fig.add_trace(go.Scatter(x= x_val, y= ddf_13(x_val), mode='lines', name='h=0.13', line=dict(color='royalblue', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= ddf_22(x_val), mode='lines', name='h=0.22', line=dict(color='teal', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= dderf(x_val), mode='lines', name='segunda derivada', line=dict(color='goldenrod', width=1)))

fig.update_layout(
    title="Gráfica de aproximación de la segunda derivada",
    xaxis_title="x",
    yaxis_title="y",
    template="plotly_white",
    width = 768,
    height = 576
)

fig.show()
```

Gráfica del valor absoluto de los errores para las aproximaciones de la segunda derivada.

```{python}
#| code-fold: true
#| warning: false

fig = go.Figure()
fig.add_trace(go.Scatter(x= x_val, y= abs(dderf(x_val)-ddf_13(x_val)), mode='lines', name='h=0.13', line=dict(color='royalblue', width=1)))
fig.add_trace(go.Scatter(x= x_val, y= abs(dderf(x_val)-ddf_22(x_val)), mode='lines', name='h=0.22', line=dict(color='teal', width=1)))

fig.update_layout(
    title="Gráfica de errores segunda derivada",
    xaxis_title="x",
    yaxis_title="y",
    template="plotly_white",
    width = 768,
    height = 576
)

fig.show()
```


# Ejericicio 2

Aproximar las siguientes integrales, cada caso hacer una gráfica de la función e indicar el área bajo la curva.

a)  

\begin{equation}
\int_0^1 e^{-x^2}\,dx
\end{equation}

```{python}
#| code-fold: true
#| fig-align: 'center'
f=lambda x: np.exp(-x**2)
a = 0
b = 1


x_val = np.linspace(a, b, 100)


plt.figure(figsize=(8,6))
plt.plot(x_val,f(x_val), label="Función")
plt.fill_between(np.linspace(a,b, 100), y1=0, y2=f(np.linspace(a,b, 100)), color="olive", alpha=0.75)
plt.grid()
plt.legend()
plt.show()
```

**Aproximación de la integral**

```{python}
#| code-fold: true

integral = integrate.quad(f, a, b)
print(integral)

```

**Aproximación de Montecarlo**

```{python}
#| code-fold: true
#| fig-align: 'center'

N = 5000
ymax = 1
ymin = 0

x = np.random.uniform(a, b, N)
y = np.random.uniform(ymin, ymax, N)

puntos_in = np.abs(y) <= abs(f(x))
puntos_in = puntos_in * np.sign(y)== np.sign(f(x))
puntos_in_pos = puntos_in *(1 == np.sign(f(x)))
puntos_in_neg = puntos_in *(0 == np.sign(f(x)))

puntos_out = ~ puntos_in
puntos_out_pos = puntos_out * (1 == np.sign(y))
puntos_out_neg = puntos_out * (0 == np.sign(y))


x_val = np.linspace(a, b, 100)

plt.figure(figsize=(8,6))
plt.plot(x[puntos_in_pos], y[puntos_in_pos], 'o', color="tan", label= "Puntos in +", alpha=0.75, markersize=2)
plt.plot(x[puntos_in_neg], y[puntos_in_neg], 'o', color="gold", label= "Puntos in -", alpha=0.75, markersize=2)
plt.plot(x[puntos_out_pos], y[puntos_out_pos], 'o', color="plum", label= "Puntos out +", alpha=0.75, markersize=2)
plt.plot(x[puntos_out_neg], y[puntos_out_neg], 'o', color="teal", label= "Puntos out -", alpha=0.75, markersize=2)
plt.plot(x_val,f(x_val), color= "gray", label="Función", linewidth=1.5)
plt.grid()
plt.legend()
plt.show()
```

```{python}
#| code-fold: true

integral_Montecarlo = (b-a)* ymax *(sum(puntos_in_pos)/(sum(puntos_in_pos) + sum(puntos_out_pos))) 

print(f'El valor aproximado con el método de Montecarlo es: {integral_Montecarlo}')
```


b)  

\begin{equation}
\int_0^\pi sen(x^2)\,dx
\end{equation}

```{python}
#| code-fold: true
#| fig-align: 'center'
f=lambda x: np.sin(x ** 2)
a = 0
b = np.pi

x_val = np.linspace(a, b, 100)

plt.figure(figsize=(8,6))
plt.plot(x_val,f(x_val), label="Función", color="darkgreen")
plt.fill_between(np.linspace(a,b, 100), y1=0, y2=f(np.linspace(a,b, 100)), color="olive", alpha=0.75)
plt.grid()
plt.legend()
plt.show()
```

**Aproximación de la integral**

```{python}
#| code-fold: true

integral = integrate.quad(f, a, b)
print(integral)

```

**Aproximación de Montecarlo**

```{python}
#| code-fold: true
#| fig-align: 'center'

N = 300000
ymax = 1
ymin = -1

x = np.random.uniform(a, b, N)
y = np.random.uniform(ymin, ymax, N)

puntos_in = np.abs(y) <= abs(f(x))
puntos_in = puntos_in * np.sign(y)== np.sign(f(x))
puntos_in_pos = puntos_in *(1 == np.sign(f(x)))
puntos_in_neg = puntos_in *(-1 == np.sign(f(x)))

puntos_out = ~ puntos_in
puntos_out_pos = puntos_out * (1 == np.sign(y))
puntos_out_neg = puntos_out * (-1 == np.sign(y))


x_val = np.linspace(a, b, 100)

plt.figure(figsize=(8,6))

plt.plot(x[puntos_in_pos], y[puntos_in_pos], 'o', color="tan", label= "Puntos in +", alpha=0.75, markersize=2)
plt.plot(x[puntos_in_neg], y[puntos_in_neg], 'o', color="gold", label= "Puntos in -", alpha=0.75, markersize=2)
plt.plot(x[puntos_out_pos], y[puntos_out_pos], 'o', color="plum", label= "Puntos out +", alpha=0.75, markersize=2)
plt.plot(x[puntos_out_neg], y[puntos_out_neg], 'o', color="teal", label= "Puntos out -", alpha=0.75, markersize=2)
plt.plot(x_val,f(x_val), color= "black", label="Función", linewidth=1.5)
plt.grid()
plt.legend()
plt.show()
```

```{python}
#| code-fold: true

integral_Montecarlo = (b-a)* ymax *(sum(puntos_in_pos)/(sum(puntos_in_pos) + sum(puntos_out_pos))) + (b-a) * ymin * (sum(puntos_in_neg)/(sum(puntos_in_neg) + sum(puntos_out_neg))) 
print(f'El valor aproximado con el método de Montecarlo es: {integral_Montecarlo}')
```


c)  

\begin{equation}
\int_0^\pi \frac{sen(x)}{x}\,dx
\end{equation}

```{python}
#| code-fold: true
#| warning: false
#| fig-align: 'center'

f=lambda x: np.sin(x)/x
a = 0
b = np.pi

x_val = np.linspace(a, b, 100)

plt.figure(figsize=(8,6))
plt.plot(x_val,f(x_val), label="Función", color="darkgreen")
plt.fill_between(np.linspace(a,b, 100), y1=0, y2=f(np.linspace(a,b, 100)), color="palegreen", alpha=0.75)
plt.grid()
plt.legend()
plt.show()
```

**Aproximación de la integral**

```{python}
#| code-fold: true

integral = integrate.quad(f, a, b)
print(integral)

```

**Aproximación de Montecarlo**

```{python}
#| code-fold: true
#| fig-align: 'center'

N = 20000
ymax = 1
ymin = 0

x = np.random.uniform(a, b, N)
y = np.random.uniform(ymin, ymax, N)

puntos_in = y <= f(x)
  
x_val = np.linspace(a, b, 100)

plt.figure(figsize=(8,6))
plt.plot(x[puntos_in], y[puntos_in], 'o', color="palegreen", label= "Puntos in", alpha=0.75)
plt.plot(x[~puntos_in], y[~puntos_in], 'o', color="khaki", label= "Puntos out", alpha=0.75)
plt.plot(x_values,f(x_values), color= "black", label="Función", linewidth=1.5)
plt.grid()
plt.legend()
plt.show()
```

```{python}
#| code-fold: true

integral_Montecarlo = (b-a)* ymax *(sum(puntos_in)/N)

print(f'El valor con el método de Montecarlo es:{integral_Montecarlo}')
```


d)  

\begin{equation}
\int_0^\infty e^{-x^2} cos(x) \,dx
\end{equation}

```{python}
#| code-fold: true
#| warning: false
#| fig-align: 'center'

f=lambda x: np.exp(-x**2)*np.cos(x)
a = 0
b = 8

x_val = np.linspace(a, b, 500)

plt.figure(figsize=(8,6))
plt.plot(x_val,f(x_val), label="Función", color="navy")
plt.fill_between(np.linspace(a,b, 500), y1=0, y2=f(np.linspace(a,b, 500)), color="moccasin", alpha=0.75)
plt.grid()
plt.legend()
plt.show()
```

**Aproximación de la integral**

```{python}
#| code-fold: true

integral = integrate.quad(f, a, np.inf)
print(integral)

```

**Aproximación de Montecarlo**

```{python}
#| code-fold: true
#| fig-align: 'center'

N = 50000
ymax = 1
ymin = 0

x = np.random.uniform(a, b, N)
y = np.random.uniform(ymin, ymax, N)

puntos_in = np.abs(y) <= abs(f(x))
puntos_in = puntos_in * np.sign(y)== np.sign(f(x))
puntos_in_pos = puntos_in *(1 == np.sign(f(x)))
puntos_in_neg = puntos_in *(0 == np.sign(f(x)))

puntos_out = ~ puntos_in
puntos_out_pos = puntos_out * (1 == np.sign(y))
puntos_out_neg = puntos_out * (0 == np.sign(y))
  
x_val = np.linspace(a, b, 500)

plt.figure(figsize=(8,6))

plt.plot(x[puntos_in_pos], y[puntos_in_pos], 'o', color="peru", label= "Puntos in +", alpha=0.75, markersize=2)
plt.plot(x[puntos_in_neg], y[puntos_in_neg], 'o', color="dimgrey", label= "Puntos in -", alpha=0.75, markersize=2)
plt.plot(x[puntos_out_pos], y[puntos_out_pos], 'o', color="plum", label= "Puntos out +", alpha=0.75, markersize=2)
plt.plot(x[puntos_out_neg], y[puntos_out_neg], 'o', color="steelblue", label= "Puntos out -", alpha=0.75, markersize=2)
plt.plot(x_values,f(x_values), color= "black", label="Función", linewidth=1.5)
plt.grid()
plt.legend()
plt.show()
```

```{python}
#| code-fold: true

integral_Montecarlo = (b-a)* ymax *(sum(puntos_in)/N)

print(f'El valor con el método de Montecarlo es:{integral_Montecarlo}')
```


e)  

\begin{equation}
\int_0^1 x^x \,dx
\end{equation}

```{python}
#| code-fold: true
#| warning: false
#| fig-align: 'center'

f=lambda x: x**x
a = 0
b = 1

x_val = np.linspace(a, b, 100)

plt.figure(figsize=(8,6))
plt.plot(x_val,f(x_val), label="Función", color="navy")
plt.fill_between(np.linspace(a,b, 100), y1=0, y2=f(np.linspace(a,b, 100)), color="cornflowerblue", alpha=0.75)
plt.grid()
plt.legend()
plt.show()
```

**Aproximación de la integral**

```{python}
#| code-fold: true

integral = integrate.quad(f, a, b)
print(integral)

```

**Aproximación de Montecarlo**

```{python}
#| code-fold: true
#| fig-align: 'center'

N = 50000
ymax = 1
ymin = -1

x = np.random.uniform(a, b, N)
y = np.random.uniform(ymin, ymax, N)

puntos_in = np.abs(y) <= abs(f(x))
puntos_in = puntos_in * np.sign(y)== np.sign(f(x))
puntos_in_pos = puntos_in *(1 == np.sign(f(x)))
puntos_in_neg = puntos_in *(-1 == np.sign(f(x)))

puntos_out = ~ puntos_in
puntos_out_pos = puntos_out * (1 == np.sign(y))
puntos_out_neg = puntos_out * (-1 == np.sign(y))
  
x_val = np.linspace(a, b, 100)

plt.figure(figsize=(8,6))

plt.plot(x[puntos_in_pos], y[puntos_in_pos], 'o', color="peru", label= "Puntos in +", alpha=0.75, markersize=2)
plt.plot(x[puntos_in_neg], y[puntos_in_neg], 'o', color="dimgrey", label= "Puntos in -", alpha=0.75, markersize=2)
plt.plot(x[puntos_out_pos], y[puntos_out_pos], 'o', color="slateblue", label= "Puntos out +", alpha=0.75, markersize=2)
plt.plot(x[puntos_out_neg], y[puntos_out_neg], 'o', color="steelblue", label= "Puntos out -", alpha=0.75, markersize=2)
plt.plot(x_values,f(x_values), color= "black", label="Función", linewidth=1.5)
plt.grid()
plt.legend()
plt.show()
```

```{python}
#| code-fold: true

integral_Montecarlo = (b-a)* ymax *(sum(puntos_in_pos)/(sum(puntos_in_pos) + sum(puntos_out_pos))) + (b-a) * ymin * (sum(puntos_in_neg)/(sum(puntos_in_neg) + sum(puntos_out_neg))) 

print(f'El valor con el método de Montecarlo es:{integral_Montecarlo}')
```


f)  

\begin{equation}
\int_1^5 e^{-x^2} x^3 dx
\end{equation}

```{python}
#| code-fold: true
#| warning: false
#| fig-align: 'center'

f=lambda x: np.exp(-x**2)*x**3
a = 1
b = 5

x_val = np.linspace(a, b, 100)

plt.figure(figsize=(8,6))
plt.plot(x_val,f(x_val), label="Función", color="crimson")
plt.fill_between(np.linspace(a,b, 100), y1=0, y2=f(np.linspace(a,b, 100)), color="orchid", alpha=0.75)
plt.grid()
plt.legend()
plt.show()
```

**Aproximación de la integral**

```{python}
#| code-fold: true

integral = integrate.quad(f, a, b)
print(integral)

```

**Aproximación de Montecarlo**

```{python}
#| code-fold: true
#| fig-align: 'center'

N = 50000
ymax = 1
ymin = -1

x = np.random.uniform(a, b, N)
y = np.random.uniform(ymin, ymax, N)

puntos_in = np.abs(y) <= abs(f(x))
puntos_in = puntos_in * np.sign(y)== np.sign(f(x))
puntos_in_pos = puntos_in *(1 == np.sign(f(x)))
puntos_in_neg = puntos_in *(-1 == np.sign(f(x)))

puntos_out = ~ puntos_in
puntos_out_pos = puntos_out * (1 == np.sign(y))
puntos_out_neg = puntos_out * (-1 == np.sign(y))
  
x_val = np.linspace(a, b, 100)

plt.figure(figsize=(8,6))

plt.plot(x[puntos_in_pos], y[puntos_in_pos], 'o', color="orchid", label= "Puntos in +", alpha=0.75, markersize=2)
plt.plot(x[puntos_in_neg], y[puntos_in_neg], 'o', color="dimgrey", label= "Puntos in -", alpha=0.75, markersize=2)
plt.plot(x[puntos_out_pos], y[puntos_out_pos], 'o', color="skyblue", label= "Puntos out +", alpha=0.75, markersize=2)
plt.plot(x[puntos_out_neg], y[puntos_out_neg], 'o', color="steelblue", label= "Puntos out -", alpha=0.75, markersize=2)
plt.plot(x_values,f(x_values), color= "black", label="Función", linewidth=1.5)
plt.grid()
plt.legend()
plt.show()
```

```{python}
#| code-fold: true

integral_Montecarlo = (b-a)* ymax *(sum(puntos_in_pos)/(sum(puntos_in_pos) + sum(puntos_out_pos))) + (b-a) * ymin * (sum(puntos_in_neg)/(sum(puntos_in_neg) + sum(puntos_out_neg))) 
print(f'El valor con el método de Montecarlo es:{integral_Montecarlo}')
```


g)  

\begin{equation}
\int_0^1 \sqrt{1-x^2} dx
\end{equation}

```{python}
#| code-fold: true
#| warning: false
#| fig-align: 'center'

f=lambda x: np.sqrt(1-x**2)
a = 0
b = 1

x_val = np.linspace(a, b, 100)

plt.figure(figsize=(8,6))
plt.plot(x_val,f(x_val), label="Función", color="rosybrown")
plt.fill_between(np.linspace(a,b, 100), y1=0, y2=f(np.linspace(a,b, 100)), color="orchid", alpha=0.75)
plt.grid()
plt.legend()
plt.axis('square')
plt.show()
```

**Aproximación de la integral**

```{python}
#| code-fold: true

integral = integrate.quad(f, a, b)
print(integral)

```

**Aproximación de Montecarlo**

```{python}
#| code-fold: true
#| fig-align: 'center'

N = 5000
ymax = 1
ymin = 0

x = np.random.uniform(a, b, N)
y = np.random.uniform(ymin, ymax, N)

puntos_in = y <= f(x)
  
plt.figure(figsize=(8,6))

plt.plot(x[puntos_in], y[puntos_in], 'o', color="dodgerblue", label= "Puntos in", alpha=0.75)
plt.plot(x[~puntos_in], y[~puntos_in], 'o', color="orange", label= "Puntos out", alpha=0.75)
plt.plot(x_values,f(x_values), color= "black", label="Función", linewidth=1.5)
plt.grid()
plt.legend()
plt.axis('square')
plt.show()
```

```{python}
#| code-fold: true

integral_Montecarlo = (b-a)* ymax *(sum(puntos_in)/N)

print(f'El valor con el método de Montecarlo es:{integral_Montecarlo}')
```


h)  

\begin{equation}
\int_0^\infty \frac{x}{e^x-1} dx
\end{equation}

```{python}
#| code-fold: true
#| warning: false
#| fig-align: 'center'

f=lambda x: x/(np.exp(x)-1)
a = 0
b = 3

x_val = np.linspace(a, b, 500)

plt.figure(figsize=(8,6))
plt.plot(x_val,f(x_val), label="Función", color="black")
plt.fill_between(np.linspace(a,b, 500), y1=0, y2=f(np.linspace(a,b, 500)), color="silver", alpha=0.75)
plt.grid()
plt.legend()
plt.show()
```

**Aproximación de la integral**

```{python}
#| code-fold: true

integral = integrate.quad(f, a, np.inf)
print(integral)

```

**Aproximación de Montecarlo**

```{python}
#| code-fold: true
#| fig-align: 'center'

N = 8000
ymax = 1
ymin = 0

x = np.random.uniform(a, b, N)
y = np.random.uniform(ymin, ymax, N)

puntos_in = y <= f(x)
  
plt.figure(figsize=(8,6))

plt.plot(x[puntos_in], y[puntos_in], 'o', color="dodgerblue", label= "Puntos in", alpha=0.75)
plt.plot(x[~puntos_in], y[~puntos_in], 'o', color="slategrey", label= "Puntos out", alpha=0.75)
plt.plot(x_values,f(x_values), color= "black", label="Función", linewidth=1.5)
plt.grid()
plt.legend()
plt.show()
```

```{python}
#| code-fold: true

integral_Montecarlo = (b-a)* ymax *(sum(puntos_in)/N)

print(f'El valor con el método de Montecarlo es:{integral_Montecarlo}')
```


i)  

\begin{equation}
\int_0^1 \frac{1}{\sqrt{x^4+1}} dx
\end{equation}

```{python}
#| code-fold: true
#| warning: false
#| fig-align: 'center'

f=lambda x: 1/(np.sqrt(x**4+1))
a = 0
b = 1

x_val = np.linspace(a, b, 100)

plt.figure(figsize=(8,6))
plt.plot(x_val,f(x_val), label="Función", color="red")
plt.fill_between(np.linspace(a,b, 100), y1=0, y2=f(np.linspace(a,b, 100)), color="tomato", alpha=0.75)
plt.grid()
plt.legend()
plt.show()
```

**Aproximación de la integral**

```{python}
#| code-fold: true

integral = integrate.quad(f, a, b)
print(integral)

```

**Aproximación de Montecarlo**

```{python}
#| code-fold: true
#| fig-align: 'center'

N = 8000
ymax = 1
ymin = 0

x = np.random.uniform(a, b, N)
y = np.random.uniform(ymin, ymax, N)

puntos_in = y <= f(x)
  
plt.figure(figsize=(8,6))

plt.plot(x[puntos_in], y[puntos_in], 'o', color="tomato", label= "Puntos in", alpha=0.75)
plt.plot(x[~puntos_in], y[~puntos_in], 'o', color="peachpuff", label= "Puntos out", alpha=0.75)
plt.plot(x_values,f(x_values), color= "black", label="Función", linewidth=1.5)
plt.grid()
plt.legend()
plt.show()
```

```{python}
#| code-fold: true

integral_Montecarlo = (b-a)* ymax *(sum(puntos_in)/N)

print(f'El valor con el método de Montecarlo es:{integral_Montecarlo}')
```


# Ejercicio 3

Aproximar las siguientes integrales dobles y triples, llevar a cabo la gráfica cuando se indique y comparar con el valor *exacto* de la integral.

a)  Realizar gráfica

\begin{equation}
\int_{-1}^{1}\int_1^2 (3y^2-x^2+5) dx dy
\end{equation}

```{python}
#| code-fold: true
#| warning: false
#| fig-align: 'center'

x = np.linspace(1, 2, 100)  
y = np.linspace(-1, 1, 100)  
X, Y = np.meshgrid(x, y)

Z = 3 * Y**2 - X**2 + 5

fig = go.Figure(data=[go.Surface(z=Z, x=X, y=Y)])

fig.update_layout(title="Gráfica de la función f(x,y)=3y^2-x^2+5",
                  scene=dict(
                      xaxis_title="x",
                      yaxis_title="y",
                      zaxis_title="f(x, y)"
                  ))
  
fig.show()
```

```{python}
#| code-fold: true
#| fig-align: 'center'

x_val = ([1, 2])
y_val1 = ([-1, -1])
y_val2 = ([1, 1])

plt.figure(figsize=(8, 6))

plt.plot(x_val, y_val1)
plt.plot(x_val, y_val2)
plt.fill_between(x_val, y1=y_val1, y2=y_val2, color="tan", alpha=0.75)
plt.title("Región de integración")
plt.grid()
plt.show()
```

**Aproximación**
```{python}
#| code-fold: true

f = lambda y, x: 3*y**2 -x**2 +5

integral = integrate.dblquad(f, 1, 2, lambda x: -1, lambda x: 1)
print(f'La proximación de la integral es: {integral[0]}, con un error aproximado de {integral[1]}')
```

**Error absoluto**
```{python}
#| code-fold: true

integral_exacta = 22/3
error_absoluto = abs(integral_exacta - integral[0])
print(f'El error absoluto es:{error_absoluto}')
```



b)  

\begin{equation}
\int_{0}^{6}\int_1^5 \sqrt{x+4y} dx dy
\end{equation}

```{python}
#| code-fold: true
#| fig-align: 'center'

x = np.linspace(1, 5, 100)  
y = np.linspace(0, 6, 100)  
X, Y = np.meshgrid(x, y)

Z = np.sqrt(X+4*Y)

fig = go.Figure(data=[go.Surface(z=Z, x=X, y=Y)])

fig.update_layout(title="Gráfica de la función f(x,y)=\sqrt(x+4y)",
                  scene=dict(
                      xaxis_title="x",
                      yaxis_title="y",
                      zaxis_title="f(x, y)"
                  ))
  
fig.show()
```

```{python}
#| code-fold: true
#| fig-align: 'center'

x_val = ([1, 5])
y_val1 = ([0, 0])
y_val2 = ([6, 6])

plt.figure(figsize=(8, 6))

plt.plot(x_val, y_val1)
plt.plot(x_val, y_val2)
plt.fill_between(x_val, y1=y_val1, y2=y_val2, color="powderblue", alpha=0.75)
plt.title("Región de integración")
plt.grid()
plt.show()
```

**Aproximación**
```{python}
#| code-fold: true

f = lambda y, x: np.sqrt(x+4*y)

integral = integrate.dblquad(f, 1, 5, lambda x: 0, lambda x: 6)
print(f'La proximación de la integral es: {integral[0]}, con un error aproximado de {integral[1]}')
```

**Error absoluto**
```{python}
#| code-fold: true

integral_exacta = (1/15)*(841*np.sqrt(29) -25*np.sqrt(5) -3124)
error_absoluto = abs(integral_exacta - integral[0])
print(f'El error absoluto es:{error_absoluto}')
```


c)  

\begin{equation}
\int_{1}^{e}\int_0^{log(x)} x^3 dx dy
\end{equation}

```{python}
#| code-fold: true
#| fig-align: 'center'

x_val = np.array([0,np.log(np.exp(1))])
y_val1 = np.array([1,1])
y_val2 = np.array([np.exp(1), np.exp(1)])

plt.figure(figsize=(8,6))
plt.plot(x_val, y_val1)
plt.plot(x_val, y_val2)
plt.fill_between(x_val, y1=y_val1, y2=y_val2, color="hotpink", alpha=0.75)
plt.title("Región de Integración")
plt.grid()
plt.show()
```

**Aproximación**
```{python}
#| code-fold: true

f = lambda y, x: x**3
a, b = 1, np.exp(1)
g = lambda y: 0 
h = lambda y: np.log(y)

integral = integrate.dblquad(f, a, b, g, h)
print(f'La aproximación de la integral es: {integral[0]}, con un error aproximado de {integral[1]}')
```

**Error absoluto**
```{python}
#| code-fold: true

integral_exacta = (3*np.exp(4)+1)/(16)
error_absoluto = abs(integral_exacta - integral[0])
print(f'El error absoluto es:{error_absoluto}')
```


d)  

\begin{equation}
\int\int_D 30ye^x dx dy
\end{equation}

Donde $D\subset \mathbb{R}^2$ es la región en la siguiente gráfica.

```{python}
#| code-fold: true
#| fig-align: 'center'

x_val = np.array([0,4])
y_val1 = np.array([0, 1])
y_val2 = np.array([0, 4])

plt.figure(figsize=(8,6))
plt.plot(x_val, y_val1)
plt.plot(x_val, y_val2)
plt.fill_between(x_val, y1=y_val1, y2=y_val2, color="firebrick", alpha=0.5)
plt.grid()
plt.show()
```

**Aproximación**
```{python}
#| code-fold: true

f = lambda y, x: 30 * y * np.exp(x)

integral = integrate.dblquad(f, 0, 4, lambda x: x/4, lambda x: x)
print(f'La aproximación de la integral es: {integral[0]}, con un error aproximado de {integral[1]}')
```

**Error absoluto**
```{python}
#| code-fold: true

integral_exacta = 225/8*(5*np.exp(4)-1)
error_absoluto = abs(integral_exacta - integral[0])
print(f'El error absoluto es:{error_absoluto}')
```


e)  

\begin{equation}
\int\int \int_B z e^{x+y} dx\, dy\, dz, \, B=[0,1] \times [0,1] \times [0,1]
\end{equation}

```{python}
#| code-fold: true
#| fig-align: 'center'

x_val = np.array([0,1])
y_val1 = np.array([0, 0])
y_val2 = np.array([1, 1])

plt.figure(figsize=(8,6))
plt.plot(x_val, y_val1)
plt.plot(x_val, y_val2)
plt.fill_between(x_val, y1=y_val1, y2=y_val2, color="gold", alpha=0.75)
plt.grid()
plt.show()
```

**Aproximación**
```{python}
#| code-fold: true

f = lambda z, y, x: z*np.exp(x+y)
lim_x = [0,1]
lim_y = lambda x: [0, 1] 
lim_z = lambda x, y: [0,1]

integral = integrate.tplquad(
    f, 
    lim_x[0], lim_x[1], 
    lambda x: lim_y(x)[0], lambda x: lim_y(x)[1], 
    lambda x, y: lim_z(x,y)[0], lambda x, y: lim_z(x,y)[1]
)
print(f'La aproximación de la integral es: {integral[0]}, con un error aproximado de {integral[1]}')
```

**Error absoluto**
```{python}
#| code-fold: true

integral_exacta = ((-1+np.e)**2)/2
error_absoluto = abs(integral_exacta - integral[0])
print(f'El error absoluto es:{error_absoluto}')
```


f)  

\begin{equation}
\int_0^1 \int_0^x \int_0^y (y+xz) dz\, dy\, dx
\end{equation}

```{python}
#| code-fold: true
#| fig-align: 'center'

x_val = np.linspace(0,1, 100)
y_val1 = np.zeros_like(x_val)
y_val2 = x_val 

plt.figure(figsize=(8,6))
for x in x_val: 
  y = np.linspace(0,x,100)
  plt.fill_between(y, 0, y, color="orchid", alpha=0.75)
  
plt.plot(x_val, y_val1)
plt.plot(x_val, y_val2)
plt.title("Región de Integración")
plt.grid()
plt.show()
```

**Aproximación**
```{python}
#| code-fold: true

f = lambda z, y, x: y + x*z

lim_x = [0,1]
lim_y = lambda x: [0, x] 
lim_z = lambda x, y: [0,y]

integral = integrate.tplquad(
    f, 
    lim_x[0], lim_x[1], 
    lambda x: lim_y(x)[0], lambda x: lim_y(x)[1], 
    lambda x, y: lim_z(x,y)[0], lambda x, y: lim_z(x,y)[1]
)
print(f'La aproximación de la integral es: {integral[0]}, con un error aproximado de {integral[1]}')
```

**Error absoluto**
```{python}
#| code-fold: true

integral_exacta = 7/60
error_absoluto = abs(integral_exacta - integral[0])
print(f'El error absoluto es:{error_absoluto}')
```


# Ejercicio 4

De [scipy.stats](@https://docs.scipy.org/doc/scipy/reference/stats.html) elige alguna distribución de probabilidad continua, realiza la gráfica y encuentra la probabilidad que la variable aleatoria tome un valor en un intervalo dado. Compara el resultado con el método `cdf`.

Como ejemplo consideraremos la distribución gamma, cuya función de densidad está dada por

\begin{equation}
f(x, a)= \frac{x^{a-1} e^{-x}}{\Gamma (a)}
\end{equation}

Gráficamos la función de densidad con un valor de $a = 1.9$.
```{python}
#| code-fold: true
#| fig-align: 'center'

from scipy.stats import gamma
a = 1.9

x_values = np.linspace(0 , gamma.ppf(0.99, a), 500)

plt.figure(figsize=(8,6))
plt.plot(x_values, gamma.pdf(x_values, a), label="Función de densidad")
plt.grid()
plt.legend()
plt.show()


```

Elegimos el intervalo $[1,3]$ para calcular la integral.
```{python}
#| code-fold: true
#| fig-align: 'center'

a1 = 1
b1 = 3

x_values = np.linspace(0 , gamma.ppf(0.99, a), 500)

plt.figure(figsize=(8,6))
plt.plot(x_values, gamma.pdf(x_values, a), label="Función de densidad")
plt.fill_between(np.linspace(a1,b1, 500), y1=0, y2=gamma.pdf(np.linspace(a1,b1, 500), a), color="green", alpha=0.5)
plt.grid()
plt.legend()
plt.show()


```

Se obtiene la integral con `integrate.quad`
```{python}
#| code-fold: true

integral = integrate.quad(gamma.pdf, a1, b1, args = (a,))

print(f'La aproximación de la integral es: {integral[0]}, con un error aproximado de {integral[1]}')
```

Ahora se obtiene el valor por medio del método `cdf` (cumulative distribution function).
```{python}
gamma.cdf(3, a) - gamma.cdf(1, a)
```


**Levy**

```{python}
from scipy.stats import levy
```

```{python}
#| code-fold: true
#| fig-align: 'center'

c = 10
loc = 0

x_values = np.linspace(0, levy.ppf(0.99, loc=loc, scale=c), 50)

plt.figure(figsize=(8, 6))
plt.plot(x_values, levy.pdf(x_values, loc=loc, scale=c), label="Función de densidad")
plt.grid()
plt.legend()
plt.title('Función de Densidad de la Distribución de Lévy')
plt.xlabel('x')
plt.ylabel('f(x)')
plt.show()
```


```{python}
#| code-fold: true
#| fig-align: 'center'

a1 = 10000
b1 = 30000

plt.figure(figsize=(8, 6))
plt.plot(x_values, levy.pdf(x_values, loc=loc, scale=c), label="Función de densidad")
plt.fill_between(np.linspace(a1, b1, 500), y1=0, y2=levy.pdf(np.linspace(a1, b1, 500), loc=loc, scale=c), color="green", alpha=0.75)
plt.grid()
plt.legend()
plt.title('Área bajo la curva de la Distribución de Lévy')
plt.xlabel('x')
plt.ylabel('f(x)')
plt.show()
```


```{python}
#| code-fold: true

integral = integrate.quad(levy.pdf, a1, b1, args=(loc, c))

print(f'La aproximación de la integral es: {integral[0]}, con un error aproximado de {integral[1]}')
```


```{python}
#| code-fold: true

resultado_cdf = levy.cdf(b1, loc=loc, scale=c) - levy.cdf(a1, loc=loc, scale=c)
print(f'El valor de la CDF en el intervalo [{a1}, {b1}] es: {resultado_cdf}')

```


